version: 2

models:
  - name: clean__disaster_data
    description: "Clean disaster data table"
    columns:
      - name: __row_hash
        description: "unique hash of each row"
        tests:
          - unique
          - not_null
